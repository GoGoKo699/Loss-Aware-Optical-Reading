name: Verify research baseline
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    if: ${{ !contains(github.event.head_commit.message, '[bootstrap]') }}
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.13.5'
      - run: python -m pip install -r requirements-recorded.txt
      - run: python scripts/verify_import.py
      - run: python -m unittest discover -s tests -v
      - run: python scripts/reproduce.py --output results/runs/ci
      - name: Ensure tracked evidence did not change
        run: git diff --exit-code && git diff --cached --exit-code
