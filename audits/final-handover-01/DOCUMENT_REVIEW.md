# Final reader, presentation, and disclosure review

Reviewed from `ba00d9ce64333ad08ef3b61108aa6df959cc5fc7` on
`audit/final-handover-01`, initially clean, on 2026-09-16. This review began
independently, then implemented the coordinator's bounded presentation repair.
It does not claim an independent review of its own implementation or duplicate
the coordinating science, unit-suite, CI, or live-browser checks.

## Result

The current 28-page reader route passes the local presentation and navigation
checks after repair. No remaining blocker was found in that route. The original
main report contained a concrete presentation defect that the coordinator also
confirmed on the live GitHub page. The preserved study/repair sources retain
their original formatting; current navigation now offers generated reading
copies with literal mathematical payloads.

The former 24-page validation scope was insufficient: it omitted `REPORT.md`
and directly relevant proof/report sources. Direct TeX parsing alone was also
insufficient: all original formulas parsed, although Markdown/HTML transport
altered some before the display engine received them.

## Defects found and bounded correction

- `REPORT.md` had two raw `j<k` subscripts at original lines 124 and 156.
  Controlled HTML parsing truncated both expressions. Its bound at lines
  131–132 lost escaped set delimiters under Markdown, producing a MathJax error.
  The coordinator reported three red mathematical panels on the live page.
  All 11 displays now use literal `math` fences; single-token fraction operands
  are explicitly braced and comparisons use `\lt`. The two vulnerable inline
  norm/set expressions use GitHub's code-wrapped inline mathematics notation.
- The report's section 7 still said device availability was unconfirmed. It now
  distinguishes the confirmed SU4/SU8 processors from unsupplied supporting
  interfaces and calibration. No numerical value or scientific claim changed.
- The supplied robustness report's original display at line 52 lost
  `\left\{...\right\}` delimiters under the controlled Markdown pipeline.
  Other supplied proof displays lost escaped spacing or visible set braces.
  The numerical repair proof's final minimum lost its visible set braces.
  Their source bytes remain unchanged. Three current copies are generated by
  [build_readable_math.py](../../scripts/build_readable_math.py):
  [robustness report](../../docs/ROBUSTNESS_REPORT.md),
  [robustness proofs](../../docs/ROBUSTNESS_PROOFS.md), and
  [numerical proof](../../docs/NUMERICAL_PROOF.md).

The generator performs only display transport changes, equivalent explicit
fraction/relation spelling, relative-link rebasing, and a source footer. The
robustness report copy omits exactly two pinned obsolete delivery-status
paragraphs, retained in the original. All scientific prose and numbers remain.
The helper pins all three original hashes, validates every source before any
write, requires explicit `--write` to rebuild, and provides nonmutating `--check`.
The primary validator now includes the main report and checks generated copies.

## Checks executed

- [Final renderer evidence](DOCUMENT_RENDER_FINAL.json): Markdown-it 14.1.0
  CommonMark with the GFM table rule, MathJax 3.2.2 base/AMS, parse5 7.2.1;
  28 current pages plus 11 linked supporting/preserved pages, 144 mathematical
  expressions, 38 tables, 226 body rows, and 344 local links/anchors. Every
  expression parses directly. Current pages have no transport, table-width,
  table-code-pipe, or navigation finding. Remaining transport findings are
  explicitly confined to preserved original sources.
- [Mathematical equivalence evidence](DOCUMENT_EQUIVALENCE.json): all 37
  original/reformatted displays produce byte-identical direct MathJax SVGs.
  These comprise 11 main-report displays and 26 displays in the three copies.
  No saved SVG was added to the repository; the evidence records hashes.
- `python scripts/build_readable_math.py --check`: PASS, all three generated
  copies agree with the pinned original sources and exact transformations.
- `python scripts/verify_handover.py`: PASS, 28 documents, 307 local links,
  33 Python-script references, 31 tables, 183 body rows, and all three copies.
- Four focused generation/preservation tests and ten handover-document tests
  pass. They exercise stale-copy rejection without mutation, refusal to rebuild
  from changed source bytes, escaped delimiter transport, and current coverage.
- `git diff --check`: PASS at the review checkpoint.

[DOCUMENT_RENDER_V2.json](DOCUMENT_RENDER_V2.json) records the original
transport findings. The earlier `DOCUMENT_RENDER.json` also contains two
overbroad code-pipe heuristic hits: SU8's mask row and the example-output row.
Both were false positives caused by matching across separate code spans;
the corrected code-span check in V2 and subsequent results reports neither.

The local renderer is not GitHub's entire pipeline or CSS. Live browser review
belongs to the coordinating audit and must be reported separately. This review
does not claim all archival Markdown has been reformatted or visually cleared.

## Scientific reader route and status

The README → chip recipe/commissioning → experiment index → theory route is
self-contained. The optional engineering/discrimination crosswalk clearly
distinguishes inspected contents/abstracts from full-text access, and supplies
local explanations rather than making external reading a prerequisite.
Both chips are consistently available. Independent optical roles, loss,
phase-reference calibration, source/readout, blinding, attempted-trial
accounting, and uncertainty remain explicit laboratory inputs.

The index's new computing-relevance section preserves the source contract. It
describes prospective photon-efficient measurement and control, distinguishes
illumination-constrained advantage from computational speedup, and excludes
claims of entangling gates, fault tolerance, total-energy savings, or an
eight-qubit processor. Native Walsh8 remains separate from the four-label
classical frontier and robustness certificates. A is a candidate contribution;
B remains an attributed derived benchmark. No laboratory data or novelty
clearance is claimed. Existing protocol budgets/penalties and P0/P1 conventions
remain intact.

Current dated source-access statements and historical numerical baselines are
chronologically consistent with this review. Historical work-order and delivery
claims were interpreted at their documented scope, not as present promises.
No current philosophy approval preamble or conversational instruction was found.

Optional polish only: the final paragraph of `CLAIM_STATUS.md` says that no
license was included in the attribution update. That is a scoped historical
statement, and the README/license/third-party notices clearly establish the
current MIT arrangement. A future editorial pass could name that earlier
integration explicitly to avoid a hurried reader mistaking it for current
license status. It was not changed in this bounded repair.

## License scope and bounded sensitive-data scan

MIT covers original project material; the README and contribution guide point
to the license and third-party notices. The notice identifies all seven tracked
MathJax equation SVGs and distinguishes font-outline licensing from ownership of
the equations or claims. The included Apache text matches the installed
MathJax 3.2.2 license byte-for-byte. Separately installed dependencies and cited
books/articles are explicitly excluded from the project's MIT grant. This is
an inventory/scope review, not a new legal opinion or fresh external license
research.

The [scan record](DOCUMENT_SENSITIVE_SCAN.json) covers 291 tracked files and
243 recursively examined archive members, including nested and numerical ZIP
containers: 505 text items and 19 containers. There were no matches for private
keys, GitHub/OpenAI/AWS/Slack credentials, credential-bearing URLs, signed URL
queries, or quoted credential assignments. No matched values were recorded.

[Classification](DOCUMENT_SENSITIVE_CLASSIFICATION.json) resolves 24 email-like
hits as four occurrences of the intended public contact (including source
strings used to generate documents) and 20 matrix-expression false positives.
The 57 home-path hits are generic CI/runtime paths. No unclassified personal
contact or home-path hit remains. Names in the copyright and research citations
are intentional attribution.

The scan covers the current tracked tree and readable archives, not all Git
history, encrypted content, binary OCR, every possible secret format, or a
guarantee of absence. The ten binary members not text-scanned are NumPy arrays
inside preserved numerical archives. No external action, publication, privacy
change, acquisition, or correspondence was performed by this reviewer.
