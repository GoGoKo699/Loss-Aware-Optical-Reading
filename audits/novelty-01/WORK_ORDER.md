# Theorem-level predecessor comparison 01

Authority: the user's instruction to proceed after verified repair 01. This is a
bounded literature/claim comparison, not a new protocol search or manuscript.

Baseline: `80c73751806f45411251711bcfb6bccebc887f0a`.
Branch: `audit/novelty-01`, created at the baseline after checking all existing
branches. This branch did not previously exist. Canonical theory, source, frozen
results, original archives, old audits, and experiment protocol remain unchanged.

## Questions

Compare the flat orthogonal-code photon preparation/receiver theorem, its
zero-error and minimum-error endpoints, the uniform-loss classical frontier,
the all-intensity coherent-mixture extension, the calibrated-map witness,
calibration perturbation and statistical wrappers, and the checkpoint-06
unequal-loss crossover with their closest primary predecessors. Preserve the
actual source/receiver classes, phase reference, uniform prior, single use,
and input-energy accounting. A theorem for a fixed ensemble is not silently
called a theorem for jointly optimized transmitter and receiver.

## Method and deliverables

Read the exact repaired claims and existing source/audit records. Read primary
full texts where available; record version, sections/equations, retrieval gaps,
and any sign or notation translation. Search beyond the existing bibliography.
Classify each result as established, direct specialization, routine corollary,
potentially distinct extension, or unresolved. A failed search is not proof of
priority. Write explicit reductions and focused reproducible checks when they
help distinguish those classes. Do not infer universal claims from finite tests.

Write the work order, report, comparison derivations, source/search inventory,
focused checks, status and limitations only under `audits/novelty-01/`.
Do not redistribute complete external articles. Include source links and precise
locators instead. A literature comparison is not a second full proof audit.

## Access and preservation

The authenticated connector provides remote repository reads/writes. Container
`git ls-remote` failed at DNS resolution, so no authenticated local checkout is
claimed. Any local arithmetic will use fresh files reconstructed from supplied
archives and matched against remote content hashes; its environment and scope
will be recorded separately. There was no pre-existing local Git worktree.
Normal branch CI may rerun the inherited repository tests; distinguish those
from newly written comparison diagnostics executed locally.

## Stop

Commit the comparison packet and report what can carry a distinct scientific
claim and what must be credited as inherited. Do not merge, alter the main claim
ledger, declare universal priority, change privacy/license, contact a laboratory,
or start a scientific extension without further authorization.
