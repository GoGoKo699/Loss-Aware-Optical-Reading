# Reader-facing integration of predecessor comparison 01

Scope: integrate the completed comparison into the repository's main reading path,
not extend the mathematics or conduct an experiment.

Base main: `80c73751806f45411251711bcfb6bccebc887f0a`.
Preserved audit parent: `eaa452615096cca9a498c10c5d492a2a74966123`.
Branch: `integration/claims-01`. The pull request records the exact verified head,
merge commit and post-merge checks; this report does not self-claim verification
of a later commit.

## Reader-facing result

The [contribution note](../../docs/CONTRIBUTIONS.md) states two candidate technical
claims: input-only retuning attains the joint photon frontier in the specified
loss/code model, and a multiclass bound excludes every allowed classical source
strategy under the declared mean budget. It credits the reading mechanism,
square-root receiver, fixed-alphabet uniform curve and binary contrast as prior
results. Candidate status and the S18/S20 coverage gaps remain explicit.

The [claims-to-tests note](../../experiment/CLAIMS_TO_TESTS.md) connects A to M1
and B to P1, and gives P2 its separate restricted-photon interpretation. It keeps
controls distinct from contribution evidence. The lab brief asks for initial
interface feedback, not hardware execution or complete engineering before review.

The README, claim ledger, source map and roadmap use the same framing. The report
adds current review context and uses GitHub-supported math delimiters. Its equations
and numerical examples are unchanged. The full acquisition protocol and templates
are unchanged. The next work order is limited to outstanding source comparisons;
it has not been executed by this integration.

## Preservation and verification design

The novelty audit is integrated byte for byte. The original import manifest,
source archive, earlier audit and repair evidence, canonical proofs, numerical
routines and frozen results are not modified. A separate
[documentation overlay](../../provenance/changes/claims-integration-01.json) checks
post-repair old hashes before accepting four document updates and two new notes.
The earlier numerical-repair ledger is preserved and its digest is checked.
The overlay's permitted paths exclude mathematical modules and the protocol.

Eight integration test groups supplement, rather than replace, the original
12 engineering and 16 repair groups. They check the hash chain and tamper rejection,
canonical source/protocol identities, the original novelty manifest, report
expressions, unchanged lab questions, local links and scope wording. They also run
the 631 predecessor translations and compare identities and tolerances with the
archived results. Standard read-only CI discovers these tests automatically.
The existing 5,261-check reproduction and historical/repaired audit runner remain
part of CI. No numerical tolerance or source/receiver class is weakened.

## Execution record and limits

Local work uses a fresh archive-based snapshot, not an authenticated Git checkout:
container Git access failed at DNS resolution. Seven canonical files and the
original import manifest were matched to remote Git blob identities. The three
inputs to the focused comparison match its pinned repaired baseline. The novelty
manifest and original archive were checked before edits; only authorized document
changes are represented by the new ledger.

Local results and environment are in [LOCAL_CHECKS.json](LOCAL_CHECKS.json).
Full remote-checkout tests, clean-tree status and merge acceptance are recorded
in the pull request and CI, separately from local arithmetic. Historical audit
records are not rewritten to imply those later runs happened during the audit.
These are document-integrity and regression checks, not a new independent proof
audit, renewed literature search or verification of physical calibration.

No complete external papers, new laboratory data, submission, release, license
choice or privacy change are introduced. Publication priority and actual hardware
feasibility remain open. The first experiment's apparatus and quantitative plan
do not become more complicated because the contribution has been narrowed.
