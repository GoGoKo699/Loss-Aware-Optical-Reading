# Integrate the predecessor comparison into the research reading path

Authority: the user's request to proceed after the completed predecessor comparison.
Base main: `80c73751806f45411251711bcfb6bccebc887f0a`.
Scientific input and branch parent: `eaa452615096cca9a498c10c5d492a2a74966123`.
Working branch: `integration/claims-01`, newly created after inspecting all branches.

## Deliverables

Update the README, claim ledger, current source map, report framing and laboratory
review links. Add a self-contained two-claim contribution note and a matching
claim-to-experiment rationale. Distinguish mathematically supported results,
inherited results, candidate contributions, and unresolved priority/hardware issues.
Keep the mechanism simple and preserve the existing single-use photon resource model.

Integrate the completed novelty audit byte for byte. Preserve all earlier audits,
repair evidence, original archives, original import manifest and frozen results.
Do not modify canonical mathematical proofs, numerical routines, acquisition
protocol, hypothesis priors, penalties or sample-count examples. A report delimiter
formatting change may improve GitHub reading without changing equations.

Any protected-document changes require an additional old/new hash ledger, chained
from the existing import/repair records. The verifier may be extended only as
needed to validate that explicit documentation overlay. Add tests for preservation,
links, the documentation ledger, and the focused predecessor translations.
Do not weaken or replace historical scientific tests.

## Validation and integration

Run the focused comparison locally using hash-matched source. Use a fresh remote
checkout for the complete regression and repair suites, plus new integration checks.
Inspect the final diff, verify preserved content, and merge through a pull request
only after its checks pass. Confirm the merged main commit and post-merge checks.
Record exact outcomes rather than asserting future CI success in advance.

Container Git access failed at DNS resolution on this task. Local work uses fresh
archive-based files matched to remote Git blob identities, not an authenticated
checkout. No local clean Git status is claimed. Remote checkout status is tested
by CI. Repository reads/writes use the authorized GitHub connector.

## Stop

Stop after verified integration. Do not start a new mathematical extension,
complete the remaining literature gaps, write a manuscript, contact the laboratory,
run hardware, change privacy, select a license, or publish a release. Prepare the
remaining source-comparison task as the next bounded work item only.
