# Integrate the predecessor comparison

Authority: the user's instruction to proceed after the completed novelty audit,
whose proposed next step was integration of the source map, narrower reader-facing
claims, a two-claim contribution note, and a laboratory-review rationale.

Starting main: `80c73751806f45411251711bcfb6bccebc887f0a`.
Preserved novelty audit: `eaa452615096cca9a498c10c5d492a2a74966123`.
Branch: `integration/novelty-01`, created from the audit after checking existing
branches. Do not reset, amend, or delete either audit branch or its evidence.

## Deliverable

Make the repository explain the same mathematics with the correct attribution.
Distinguish (A) the joint normalized-input/fixed-receiver result from a known
measurement for a fixed ensemble and (B) the all-classical-source bound from a
comparison against one laser receiver. Give standard ingredients their credit.
Keep priority, calibration, and actual experimental performance unestablished.

Update the README, claim/source navigation, current scientific report framing,
roadmap, and short lab-review entry. Add `docs/CONTRIBUTIONS.md` and
`experiment/REVIEW_RATIONALE.md`. Preserve the actual first-experiment protocol,
source code, proofs, numerical contracts, raw templates, and frozen results.
Do not resolve remaining source gaps by assertion or start a new literature study.

## Integrity and validation

Preserve the original archive and import manifest. Record any approved protected
DOCUMENT edits in a new old/new hash ledger chained after repair 01. Make only
the minimal verifier change needed to validate that chain; do not authorize
numerical source or proof changes through a documentation ledger. Add tests for
hash-chain, audit preservation, reader-facing links, and required claim limits.

Run focused predecessor checks from the preserved audit on hash-matched source.
Use read-only GitHub CI for the full inherited and repaired regression suites.
Inspect the actual PR diff and CI status before a history-preserving merge to
main; check main again afterward. No release, license, privacy change, external
message, laboratory execution, or manuscript is authorized.

## Access record

Repository access uses the authenticated GitHub connector. This turn's container
`git ls-remote` failed to resolve github.com, so no authenticated local clone or
local clean Git worktree is claimed. Local checks use fresh extracted files,
matched to available remote Git blob identities. CI reports its own checkout
commit and clean-tree status. Old logs are not presented as new executions.

## Stop

Stop after integration, evidence recording, and verified main CI. Report the
exact commit, checks actually run, and remaining source/calibration gaps. Prepare
no new scientific work order as though the user had already authorized it.
