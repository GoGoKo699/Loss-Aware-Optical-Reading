# Full-text attribution integration 02

This task integrates the completed source follow-up. It adds no scientific
claim, changes no numerical routine or canonical proof, and changes no experiment.
A remains the leading candidate theoretical contribution. B remains the strong
classical-source benchmark, now explicitly presented as a derived optical corollary.

Starting main: `db17e1ec1868107b0fe1042d5a480769b552633e`.
Preserved audit: `ca0480eec0064dbcdf50ff3d60032022deff5d3c`.
Existing integration checkpoint: `949df32b69f95576534ca401f5d958ddb09d55b9`.
Continuation marker: `4d0e9e367943f23db578312503e603b17e1c63e0`.
Branch: `integration/novelty-02`. The branch was continued without restarting it.
The separate unfinished `audit/fulltext-crosscheck-02` branch was not modified.

## Reader-facing result

The README, claim ledger, contribution note, source map, scientific report,
roadmap, laboratory rationale/brief, and current task boundary use the same
hierarchy. S20's zero-error relation, S22's duality relation, S08's conclusive
filter and changed priors, and the mean-energy optical reduction are credited.
The named S18/S20 access tasks are resolved at their recorded levels, not
relabelled as universal priority clearance. Partial additional leads stay visible.

The exact main REPORT.md was reconstructed from the supplied checkpoint plus
its retrieved documentation changes and matched to Git blob
`19e17833e6146e5c51353c5275f2d7aa140c2919` before editing. The new edits only update
attribution/reading labels. All eleven display-equation bodies and the original
five lab questions are preserved exactly. No mathematical or operating value
is changed. The lab rationale changes the interpretation, not M1/P1/P2's rules.

## Preserve two distinct follow-up records

The existing committed audit contains 572 checks and remains untouched under
`audits/novelty-02/`. It uses the reweighted conclusive-filter reduction.
The separately supplied packet has 717 assertions and an additional failure-row
argument. Its source ZIP SHA-256 is
`e0e6558b99d15451ed02388dee3a579e03b6f37dc906d87d7cec2287fcd6f557`.

All original audit-directory members of that supplied packet are preserved
byte for byte in [SUPPLIED_FOLLOWUP.tar.xz](SUPPLIED_FOLLOWUP.tar.xz), identified
by [SUPPLIED_MANIFEST.json](SUPPLIED_MANIFEST.json). Their original directory
names collide with the committed audit, so the archive is not unpacked over it.
The old text saying it was local/uncommitted describes that historical delivery;
it is not the current integration status. The outer transport patch is not
reapplied. No third-party full articles are redistributed.

Do not add 572 and 717 as if they were a single scientific validation or proof.
They have separate scripts, seeds, groups, results, environments, and source
histories. Both support the same attribution classification.

## Integrity and tests

Canonical `src/`, `proofs/`, the operational `FIRST_EXPERIMENT.md`, physical
setting, original archives/import manifest, old hash ledgers, templates, frozen
results, prior audit/repair files, and all existing test files are unchanged.
Only the existing integrity verifier's document-chain handling is extended.

A third old/new layer, [novelty-integration-02.json](../../provenance/changes/novelty-integration-02.json),
follows the two existing ledgers. It authorizes exactly the four existing
protected documents plus the two existing notes, never numerical modules, proofs,
new paths, or a source-archive refresh. The old verifier outputs used by earlier
tests remain compatible. Fresh preimage, duplicate, reason, path-set, and tamper
checks guard this layer.

Ten new unit-test groups cover the chain, the unchanged old ledgers, both
follow-up evidence sets, the corrected contribution hierarchy, and execution of
the 572-check and separate 717-assertion diagnostics. Unlike the historical audit
branches, the integrated normal unittest discovery runs both focused diagnostics.
Old scientific tolerances and assertions are not changed. The inherited 37 unit
groups, 5,261-check reproduction, 998 historical audit checks, and 993 repaired
science cases remain in the normal read-only CI.

## Actual local checks

[LOCAL_CHECKS.json](LOCAL_CHECKS.json) records fresh standalone runs of both
scripts. The 572-check run passed with maximum residual
`1.2168044349891716e-13`; the 717-assertion run passed with maximum residual
`6.702971511174383e-15`. Each RESULTS.json matched its own archived result bytes.
The local integration test rerun independently repeated both scripts.

Five self-contained integration unit groups passed locally. Other groups require
the full repository and are not claimed locally. Static checks also passed for
the eleven report equations, five questions, and reader math delimiters.
An initial new documentation test expected the term `conclusive-filter`; the
source map used `Conclusive filtering`. The map was clarified, and the unchanged
assertion then passed. The initial and passing logs are retained; no scientific
check or tolerance was loosened.

Git access in the container failed at DNS resolution. These are extracted-file
checks, not an authenticated local clone or complete local working-tree audit.
The authenticated connector is used for repository changes. Complete-checkout
CI and clean-tree evidence must be inspected at their actual head/merge commits.
The final commit and outcomes are recorded in the integration PR, not predicted
by this report. A configured workflow alone is not a completed run.

## Completion boundary

Review the full diff, preserve audit history on merge, and inspect post-merge
verification before calling the task complete. Do not overwrite concurrent work.
This integration does not repeat the source search, assess A's publication
significance, contact a laboratory, change hardware, or collect data. It does not
change privacy, select a license, draft a manuscript, or publish a release.

The next bounded scientific decision concerns A's significance and physically
relevant robustness. Initial lab feedback remains a separate path. Neither a
valid derived benchmark nor passing software checks establishes a measured
advantage, worldwide priority, or a journal outcome.
