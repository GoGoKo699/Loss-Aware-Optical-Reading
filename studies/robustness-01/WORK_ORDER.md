# Bounded robustness study of A

Authority: the user approved the focused pre-experiment tolerance check after
attribution integration 02. Baseline main: `de73c9b094036b756d1ff008eaccbc52cec46207`;
tree `a072bbe43effca4c99ea7839c6802c530ac94099`. New branch:
`research/robustness-01`, created at that exact baseline after branch inspection.
No earlier branch is reset or reused.

## Fixed scope

Four tested paths, one computational photon, one traversal per fresh uniform
hidden label, primary payoff C-5E. No occupied bypass, idler, multiphoton computing,
quantum memory, recirculation, or fifth healthy hypothesis. Retain the existing
classical source class, energy planes, phase-reference and all-attempt rules.
No laboratory calibration or lab-confirmed dominant error ranges have been
supplied. All numerical error ranges in this task are hypothetical sensitivity
screens, not hardware specifications or acceptance criteria.

Distinguish known diagonal loss and common calibratable input/output basis
changes from residual phase-code errors and mode mixing/distributed loss that
break the ideal model. Compare feasible fixed-decoder performance against a
valid bound on all allowed input/measurement strategies for the same device.
Separate actual loss of information, fixed-receiver regret, and implementation
error. Lack of a better receiver in a local search is not an optimality proof.

## Deliverables

Write a small set of analytic tolerance statements, reproducible four-mode
sensitivity calculations, independently checkable upper/lower certificates when
possible, and a calibration-facing decision worksheet. Use the planned positive
and mechanism-test loss points as hypothetical examples. A coarse universal
bound may be supplemented by stronger structure-dependent bounds; neither is
called tight without an attaining strategy. Treat the existing classical
certificate as credited derived theory, not new generic discrimination algebra.

Only add files under `studies/robustness-01/`. Do not change canonical proofs,
source, tests, acquisition protocol, lab questions, claim/source ledgers, old
audits, archives, templates or frozen results. This is not a new manuscript,
worldwide novelty search, source procurement, laboratory contact, experiment,
license, release or privacy change. Do not merge without separate integration
review. A useful bounded result can be completed without solving the entire
arbitrary-noise frontier or selecting final experimental tolerances.

## Execution and evidence

The authenticated GitHub connector supplies remote reads/writes. Container Git
access was attempted and failed at DNS resolution; there is no authenticated
local Git checkout and no local dirty-status claim. Fresh local work uses a
new directory. Selected baseline source extracted from the supplied repair
archive has Git blob identities matching the recorded baseline; any imported
source must be checked against current remote identities. Independently written
code and fresh outputs must be identified separately from historical validation.
Record scripts, inputs, environments, failed searches, tolerances, mathematical
versus numerical guarantees, and result hashes. Never overwrite archived runs.
Inspect final remote tree/ref and actual branch CI; distinguish inherited CI
checks from newly executed local diagnostics. Stop after committing the bounded
study and reporting what still requires laboratory calibration.
